<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wangshiqi.bestgift">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ui.app.BestGiftApp"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 欢迎页 -->
        <activity android:name=".ui.activity.WelcomeActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".ui.activity.RotateDetailActivity" />
        <activity android:name=".view.ImageShower"
            android:theme="@style/SelectionRvDetailStyle"/>
        <activity android:name=".ui.activity.SelectionLvDetailActivity"/>
        <activity android:name=".ui.activity.SearchActivity"/>
        <activity android:name=".ui.activity.GiftDetailActivity"/>
        <activity android:name=".ui.activity.TaobaoShoppingActivity"/>
        <activity android:name=".ui.activity.LoginActivity"/>
    </application>

</manifest>